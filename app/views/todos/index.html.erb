<div class="grid-x align-middle">
  <div class="auto cell">
    <h2>Todo List</h2>
  </div>
  <div class="shrink cell ">
    <%= link_to "New Todo", new_todo_path, class: "button" %>
  </div>
</div>

<% @todos.each do |todo| %>

<% end %>
